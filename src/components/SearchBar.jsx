import React from 'react'

const SearchBar = () => {
  return (
    <input
      type="text"
      placeholder="Search category"
      className="w-full p-3 border rounded-full shadow"
    />
  );
}

export default SearchBar